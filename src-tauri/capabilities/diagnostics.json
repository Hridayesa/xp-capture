{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "diagnostics",
  "description": "Minimal capability for runtime diagnostics and explicit camera scan commands.",
  "windows": ["main"],
  "permissions": []
}
