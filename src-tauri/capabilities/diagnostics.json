{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "diagnostics",
  "description": "Minimal capability for the camera-independent runtime diagnostic screen.",
  "windows": ["main"],
  "permissions": []
}
